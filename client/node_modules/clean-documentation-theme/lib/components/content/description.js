'use strict';

var React = require('react');
var Radium = require('radium');

var _require = require('../styles'),
    lineHeight = _require.lineHeight;

var Utils = require('../../utils');

var Description = function Description(_ref) {
  var content = _ref.content,
      utils = _ref.utils;

  var rendered = {
    __html: utils.md(content)
  };

  var style = {
    marginTop: lineHeight(0.5),
    marginBottom: lineHeight(0.5)
  };

  return React.createElement('div', {
    dangerouslySetInnerHTML: rendered,
    style: style });
};

Description.propTypes = {
  content: React.PropTypes.oneOfType([React.PropTypes.string, React.PropTypes.object]).isRequired,
  utils: React.PropTypes.instanceOf(Utils).isRequired
};

module.exports = Radium(Description);